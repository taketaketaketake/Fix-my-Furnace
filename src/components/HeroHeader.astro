---
/**
 * HeroHeader.astro
 * Split hero layout – full-width image left, text right.
 * Mobile: text appears above the image.
 */
const {
  title = "Built on Trust, Speed, and Real Detroit Grit.",
  description = "We’re not a call center — we’re your local HVAC pros. When your furnace breaks, we move fast, work clean, and don’t leave you guessing. Every job is done with care, precision, and old-school Detroit craftsmanship.",
  primaryBtnText = "Get a Free Quote",
  primaryBtnLink = "/landing_page_1",
  secondaryBtnText = "Call Now",
  secondaryBtnLink = "tel:+13135551234",
  imageSrc = "/Fix_My_Furnace_Hero_Image.png"
} = Astro.props;
---

<section class="relative w-full bg-white overflow-hidden">
  <div class="flex flex-col-reverse md:grid md:grid-cols-2 min-h-[600px] lg:min-h-[700px]">

    <!-- 🧱 Left: Text -->
    <div class="flex items-center justify-center px-6 md:px-12 py-16 md:py-0 bg-gradient-to-br from-red-50 via-gray-50 to-white">
      <div class="max-w-lg text-left">
        <h1 class="text-4xl md:text-5xl font-extrabold text-gray-900 mb-6 leading-tight tracking-tight">
          <span class="text-red-600">Built on Trust, Speed,</span><br />
          and <span class="underline decoration-red-600">Real Detroit Grit.</span>
        </h1>

        <p class="text-gray-700 text-base md:text-lg font-semibold leading-relaxed mb-8">
          We’re not a call center — we’re your local HVAC pros.
          When your furnace breaks, we move fast, work clean, and don’t leave you guessing.
          Every job is done with care, precision, and old-school Detroit craftsmanship.
        </p>

        <div class="flex flex-wrap gap-4">
          <a
            href={primaryBtnLink}
            class="bg-red-600 text-white px-6 py-3 rounded-lg font-bold shadow hover:bg-red-700 active:scale-[0.98] transition"
          >
            {primaryBtnText}
          </a>
          <a
            href={secondaryBtnLink}
            class="border-2 border-gray-900 text-gray-900 px-6 py-3 rounded-lg font-bold hover:bg-gray-900 hover:text-white transition"
          >
            {secondaryBtnText}
          </a>
        </div>
      </div>
    </div>

    <!-- 🔧 Right: Image -->
    <div class="relative w-full h-[300px] md:h-auto">
      <img
        src={imageSrc}
        alt="Furnace technician at work"
        class="absolute inset-0 w-full h-full object-cover object-center"
        loading="eager"
      />
      <!-- subtle overlay for text balance -->
      <div class="absolute inset-0 bg-gradient-to-l from-black/20 to-transparent md:hidden"></div>
    </div>
  </div>

  <!-- Decorative bar for depth -->
  <div class="absolute bottom-0 left-0 w-full h-2 bg-red-600"></div>
</section>
